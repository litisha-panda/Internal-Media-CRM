# ── Local development environment ──────────────────────────────────────────
# Copy this file to .env and fill in your values:
#   cp artifacts/otv-platform/.env.example artifacts/otv-platform/.env

# URL of the local API server (used by the Vite proxy in vite.config.local.ts)
VITE_API_URL=http://localhost:3000
