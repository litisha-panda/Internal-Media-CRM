Fix the following four issues:

1. ADMIN WIZARD STILL WRONG
Open FirstLoginWizard.tsx. Find where the component decides which steps to show based on user role.
The Admin role (role === 'ADMIN' or role === 'ADMIN') must show ONLY:
  - Step 1: Full Name text input only. No region dropdown. No reporting manager dropdown.
  - Step 2: Table of unassigned reps (manager_id is null) with a Reports To dropdown per row
  - No tour on completion. Just close the wizard.
Remove region and reporting manager from the Admin step 1 entirely.
Remove target upload from the Admin wizard entirely.
Remove the tour trigger from the Admin wizard completion handler.
Log console.log("role is:", userRole) at the top of FirstLoginWizard to confirm what role value is being received.

2. INVITE LINK NOT GENERATING
In AdminView.tsx, find the Invite User modal submit handler.
Log the full API response to console when the invite button is clicked.
Check that POST /api/auth/invite (or /api/auth/invite-token) is registered in the API server's route index.
Check artifacts/api-server/src/routes/auth.ts for the invite token POST handler — does it exist and return a token?
Fix any broken route or missing registration. The modal must display a copyable link in format: https://mediacrm.replit.app?invite=TOKEN

3. REGION HEAD DROPDOWN EMPTY (Sales Rep wizard Step 1)
The dropdown should show all users with role = 'REGION_HEAD'.
Check: does GET /api/users return role in underscore format (REGION_HEAD) or space format (REGION HEAD)?
In FirstLoginWizard.tsx, fix the filter to match whatever format the API returns.
Run: psql $DATABASE_URL -c "SELECT name, email, role FROM otv_users WHERE role = 'REGION_HEAD';"
If no REGION_HEAD users exist, that is the real problem — confirm and report back.

4. CSV SUBMISSION FAILING (Sales Rep wizard Step 2)
Add console.log statements to the CSV parse function and the submit handler in FirstLoginWizard.tsx.
Log: parsed row count, first parsed row object, and the exact payload being sent to the API.
Check the API response when submitting — is it returning an error?
Fix whatever is causing the submission to fail silently.

TypeScript check after all changes — 0 errors expected.